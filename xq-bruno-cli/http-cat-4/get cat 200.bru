meta {
  name: get cat 200
  type: http
  seq: 1
}

get {
  url: https://http.cat/status/200
  body: none
  auth: inherit
}

tests {
  test('check status', () => {
    expect(res.getStatus()).equal(200)
  })
}

settings {
  encodeUrl: true
}
