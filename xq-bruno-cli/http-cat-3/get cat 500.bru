meta {
  name: get cat 500
  type: http
  seq: 1
}

get {
  url: https://http.cat/status/500
  body: none
  auth: inherit
}

tests {
  test('check status', () => {
    expect(res.getStatus()).equal(200)
  })
}

settings {
  encodeUrl: true
}
